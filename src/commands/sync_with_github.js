function syncWithGithub(params = {}){
  let briefcase = this
  let data = {}

  briefcase.log("syncing with github", data = {})
}

syncWithGithub.label = "syncWithGithub"
syncWithGithub.natural = "sync with github"

module.exports = syncWithGithub
